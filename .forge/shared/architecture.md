# Architecture

> Fill in this document to give Forge agents a shared mental model of the codebase.
> Keep it current — agents read this before making architectural decisions.

## Module Map

<!-- List the major modules/packages/directories and their purpose.
     Example:
     - `src/auth/`       — Authentication and authorization
     - `src/api/`        — REST API handlers
     - `src/db/`         — Database access layer
     - `src/workers/`    — Background job processors
-->

_Not yet documented._

## Key Patterns

<!-- Describe the primary design patterns in use and where they appear.
     Example:
     - Repository pattern for data access (see `src/db/`)
     - Event-driven updates via internal message bus (see `src/events/`)
     - Feature flags via environment config (see `src/config/flags.ts`)
-->

_Not yet documented._

## Dependency Graph

<!-- Describe or diagram module dependencies.
     Highlight which modules are shared, which are leaf nodes, and
     which have strict layering rules (e.g., "db/ must not import api/").
-->

_Not yet documented._

## External Dependencies

<!-- List key external services and libraries the system depends on.
     Example:
     - PostgreSQL (primary datastore)
     - Redis (caching + queues)
     - Stripe (payments)
-->

_Not yet documented._
