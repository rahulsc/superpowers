# Conventions

> Agents read this before writing code, naming things, or creating commits.
> Keep it specific and actionable — avoid vague statements like "write clean code".

## Naming

<!-- How to name files, classes, functions, variables, constants, etc.
     Example:
     - Files: kebab-case for scripts, PascalCase for classes (`UserService.ts`)
     - Variables: camelCase in JS/TS, snake_case in Python
     - Database tables: plural snake_case (`user_sessions`)
     - Environment variables: SCREAMING_SNAKE_CASE
-->

_Not yet documented._

## Style

<!-- Code style rules beyond what the linter enforces.
     Example:
     - Max function length: 50 lines
     - Prefer early returns over nested conditionals
     - No inline TODOs — open a ticket instead
     - Avoid `any` type in TypeScript
-->

_Not yet documented._

## Testing

<!-- Testing conventions: what to test, how to structure tests, naming.
     Example:
     - Unit tests alongside source files (`foo.test.ts` next to `foo.ts`)
     - Integration tests in `tests/integration/`
     - Test names: "describe what it does, not how" (`it("rejects expired tokens")`)
     - Always test the happy path + one error path minimum
-->

_Not yet documented._

## Commit Conventions

<!-- How to write commit messages.
     Example:
     - Format: `type(scope): short description` (Conventional Commits)
     - Types: feat, fix, chore, docs, test, refactor
     - Body: explain WHY, not WHAT (the diff shows what)
     - Reference issues: `Closes #123`
-->

_Not yet documented._

## Branch and PR Conventions

<!-- How to name branches, open PRs, and request reviews.
     Example:
     - Branch names: `feat/short-description`, `fix/issue-123`
     - PR titles: same format as commit messages
     - Required reviewers: at least one team member outside the author's squad
-->

_Not yet documented._
